# card-news-simple

인스타그램 카드뉴스(4:5, 1080x1350) 생성기. 클로드 코드에 대화로 요청하면 카드별 HTML을 작성하고 Playwright로 PNG를 캡처한다.

## 워크플로우

사용자가 **주제 한 줄** 또는 **완성 원고**를 주면:

1. **톤 확인**: 이번 게시글의 톤(차분한 설명체 / 훅·임팩트 중심 / 감성 에세이 등)을 사용자에게 확인한다. 사용자가 이미 지정했으면 묻지 않는다.
2. **카피 작성**: 표지 1장 + 본문 3~5장 + 마무리 1장, 총 5~7장 구조로 카드별 카피를 작성한다.
   - 원고를 받은 경우: 내용을 카드 단위로 분할·압축만 하고 새 주장을 추가하지 않는다.
   - 본문 카드 밀도: 제목 + 3~4줄 (중밀도)
   - 표지: 저밀도, 큰 타이포 훅 / 마무리: 요약 또는 행동 유도 + 팔로우 유도
3. **HTML 작성**: `output/<주제-슬러그>/` 아래 `01.html`, `02.html`... 카드별 파일 생성. 공통 스타일은 `templates/base.css`를 상대경로(`../../templates/base.css`)로 링크.
4. **캡처**: `node scripts/capture.mjs output/<주제-슬러그>` 실행 → 같은 폴더에 `01.png`, `02.png`... 생성.
5. **보고**: 생성된 PNG 목록과 경로를 사용자에게 알린다. HTML은 삭제하지 않는다(문구 수정 후 재캡처용).

## 디자인 규칙 (절대 규칙)

- 캔버스: 1080 x 1350px (4:5)
- 배경 `#000000`, 텍스트 `#ffffff`만 사용. **컬러 금지.** 회색 계조(`#888`, `#aaa` 등)는 보조 텍스트·구분선에 허용.
- 폰트: Pretendard (base.css에서 CDN 로드). 다른 폰트 금지.
- 타이포그래피 중심. 이미지·일러스트·이모지 사용 금지. 구분선, 번호, 큰따옴표 같은 타이포 장치는 허용.
- 레이아웃은 카드마다 자유 구성: 좌상단 정렬, 중앙 정렬, 세로 리듬, 큰 숫자, 인용 블록 등 카드 역할에 맞게 변주하되 게시글 전체의 통일감(여백 체계, 페이지 번호 위치 등)은 유지한다.
- 안전 여백: 모든 텍스트는 가장자리에서 최소 80px 안쪽에.
- 페이지 표시: 본문 카드 우하단에 `02 / 06` 형식 (표지는 생략 가능).
- 표지 카드에는 다음 장으로 넘기게 만드는 훅 한 줄을 넣는다.

## HTML 카드 골격

```html
<!DOCTYPE html>
<html lang="ko">
<head>
<meta charset="UTF-8">
<link rel="stylesheet" href="../../templates/base.css">
<style>/* 이 카드 전용 레이아웃 */</style>
</head>
<body>
  <div class="card">
    <!-- 자유 구성 -->
  </div>
</body>
</html>
```

## 폴더 구조

```
templates/base.css        # 공통 스타일 (캔버스, 폰트, 리셋)
scripts/capture.mjs       # HTML → PNG 일괄 캡처
output/<주제-슬러그>/      # 게시글별 산출물 (NN.html + NN.png)
```

## 명령어

- 캡처: `node scripts/capture.mjs output/<폴더>` (폴더 내 *.html 전부 → 같은 이름 .png)
- 의존성: `npm install` (playwright). 최초 1회 `npx playwright install chromium`
