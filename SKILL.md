---
name: card-news-simple
description: >-
  주제 한 줄(또는 완성 원고)을 받아 인스타그램용 4:5 카드뉴스 PNG(1080x1350) 5~7장을
  만들어 주는 템플릿 스킬. 검정 배경·흰 글씨·Pretendard 자간 -3px의 흑백 타이포 베이스라인에서
  출발해, 사용자 인터뷰와 레퍼런스 이미지(닮고 싶은 카드뉴스 캡처)를 받아가며
  그 사람만의 디자인으로 길들여 가는 것이 핵심. 원고를 받으면 내용·사실·수치는
  한 글자도 날조하지 않고 카드 단위 분할·압축만 한다.
  필수 사용이 아니라 "카드뉴스로 만들까요? (이 스킬 사용)"라고 먼저 묻는 옵트인 스킬.
  트리거 — "카드뉴스 만들어줘", "인스타 카드뉴스", "이 글 카드뉴스로", "4:5 카드 이미지",
  "인스타 게시글 이미지로", "card news". 단일 이미지 생성·일반 PPT는 이 스킬이 아니다.
---

# card-news-simple

주제(또는 원고) → **흑백 타이포 카드뉴스 PNG 5~7장**. 핵심 철학: **내용은 날조하지 않고, 디자인은 베이스라인에서 출발해 그 사용자에게 맞춰 진화한다.**

## 이 스킬의 성격 (필독)

- **옵트인**: 자동으로 쓰지 마라. "카드뉴스로 만들까요? (이 스킬 사용)"라고 **먼저 묻고** 동의할 때만 쓴다.
- **Generator를 도와주는 스킬**: 이 스킬의 역할은 카드뉴스 한 벌을 뽑아주는 데서 끝나지 않는다. 인터뷰와 피드백으로 **그 사용자가 카드뉴스를 잘 만들도록 도와주는 것**이 본질이다.
- **진화하는 디자인(기본값)**: 디자인은 고정이 아니다. 사용자 피드백을 받을 때마다 `templates/base.css`와 `CLAUDE.md`의 디자인 규칙을 조금씩 발전시킨다. **첫 카드뉴스를 만드는 순간이 이 스킬이 "그 사용자의 스킬"로 길들여지기 시작하는 시점**이다. (통일성은 base.css + 디자인 절대 규칙이 보장 — 바뀌는 건 디자인 자체이지 통일 방식이 아니다)
- **레퍼런스 흡수**: 사용자가 닮고 싶은 카드뉴스 캡처(스크린샷·사진)를 주면 `references/`에 보관하고, 이미지를 직접 읽어 타이포 위계·여백·정렬·밀도·장치(구분선, 번호, 인용 표기 등)를 분석한 뒤 base.css와 디자인 규칙에 반영한다. 단, **컬러 도입 등 베이스라인의 큰 전환은 반드시 사용자에게 확인 후** 진행한다.
- **템플릿 스킬 → 분화**: 이건 출발점 템플릿이다. 사용자가 결과에 충분히 만족하는 시점이 오면 `simple` 딱지를 떼고 **"이 스킬을 뭐라고 기억할까요?"**라고 물어, 답한 이름으로 **새 스킬로 분화(포크)**시켜라.

## 아키텍처 (이걸 지켜야 통일성이 안 깨진다)

- **너(Claude)의 역할**: 카피를 쓰고(또는 원고를 분할하고) → 카드별 HTML을 만든다. 공통 토대(캔버스·폰트·자간·여백·페이지 번호)는 절대 직접 정의하지 않는다.
- **`templates/base.css`의 역할**: 통일성의 단일 원천. 캔버스 1080x1350, Pretendard, 자간 -3px, 안전 여백, 공통 장치가 전부 여기서 고정된다.
- 카드별 `<style>`은 레이아웃 변주(정렬·크기·리듬)만 담당한다. base.css가 정한 것을 카드에서 재정의하지 마라.
- 상세 워크플로우·디자인 절대 규칙은 `CLAUDE.md`를 따른다.

## 온보딩 (config.json의 `onboarded`가 false일 때만, 1회)

처음 실행이면 카드뉴스를 만들기 전에 사용자에게 묻고 답을 `config.json`에 저장한 뒤 `onboarded: true`로 바꾼다. (AskUserQuestion 사용, 쉬운 말로)

1. **레퍼런스**: "닮고 싶은 카드뉴스 캡처가 있나요? 있으면 `references/`에 넣거나 지금 보여주세요. 없으면 기본 흑백 타이포 스타일로 시작합니다." → 받으면 분석해 디자인 규칙에 반영.
2. **디자인 확인**: "검정 배경 + 흰 글씨 + Pretendard 흑백 타이포 스타일 그대로 괜찮나요?" (자간·최소 글씨 크기 같은 세부는 쓰면서 다듬는다고 안내)
3. **기본 톤**: "자주 쓸 톤이 있나요? (차분한 설명체 / 훅 중심 / 감성 에세이 / 매번 그때그때)" → 답을 `tone_default`에 저장. "매번"이면 생성 때마다 묻는다.
4. **계정 표기**: "카드에 계정 핸들이나 워터마크를 넣을까요? (예: @handle 우상단)" → 넣으면 base.css 공통 장치로 추가.

온보딩을 건너뛰라고 하면 기본값으로 진행하고 `onboarded: true`로만 바꾼다.

## 워크플로 요약 (상세는 CLAUDE.md)

1. 톤 확인(tone_default 없으면) → 2. 카피 작성(표지 1 + 본문 3~5 + 마무리 1) → 3. `output/<슬러그>/NN.html` 작성 → 4. `node scripts/capture.mjs output/<슬러그>` → 5. PNG를 직접 읽어 육안 검수(겹침·넘침·여백·규칙 위반) → 6. 보고.

- **환경 준비는 사용자 블랙박스**: `node_modules` 없으면 `npm install`, Chromium 없으면 `npx playwright install chromium`을 조용히 실행. 실패할 때만 알린다.
- **검수에서 걸리면 스스로 고치고 재캡처**한 뒤 보고한다.

## 충실도 철칙 (원고 입력 시 · 위반 금지)

- **사실·수치·고유명사·인용·인과관계·순서는 추가/삭제/변형 0.**
- 허용: 카드용 **문장 압축·제목화·재배열·요점화** (의미 보존 하에)
- 원문이 빈약하면 카드도 담백하게. 채우려고 환각하지 마라. (주제만 받은 경우는 카피 창작이 일이므로 해당 없음 — 단, 실존 통계·인용을 지어내는 것은 그때도 금지)

## 서브에이전트 정책

- **기본은 단일 패스**(너 혼자 처리). 카드 7장 이하는 서브에이전트를 **띄우지 않는다.**
- 사소한 수정·재캡처에 서브에이전트를 띄우는 것은 금지(오버헤드).

## 절대 규칙 요약

1. 원고 모드에서 사실/수치 날조 금지.
2. 공통 토대는 base.css가 단일 결정. 카드에서 재정의 금지.
3. 디자인 절대 규칙(CLAUDE.md): 흑백, Pretendard, 자간 -3px, 최소 폰트 32px, 최대/최소 3:1, 안전 여백 80px.
4. 한 카드 한 메시지, 넘치면 장을 나눈다(디자인 타협 금지).
5. 통일 방식(base.css + 규칙 문서)은 고정. 단, **디자인 자체는 사용자 피드백·레퍼런스로 계속 진화**시킨다.
6. 파이프라인 관련 사용자 입력은 `prompt_log.txt`에 원문 그대로 append (극사실주의).

## 파일

- `CLAUDE.md` — 상세 워크플로우·디자인 절대 규칙 (Claude용)
- `templates/base.css` — 통일성의 단일 원천
- `scripts/capture.mjs` — HTML → PNG 캡처
- `config.json` — onboarded·tone_default·handle·레퍼런스 반영 이력
- `references/` — 사용자가 준 닮고 싶은 카드뉴스 캡처 보관
- `output/<슬러그>/` — 게시글별 HTML + PNG
- `README.md` — 사용자용 최소 가이드 / `prompt_log.txt` — 제작 과정 원문 기록
